# Buku Doa Harian

228 doa & dzikir Islam dari [EQuran.id](https://equran.id) — Arab, transliterasi, terjemahan.

**Stack:** Rust + Axum (backend) | Vue 3 + Vite (frontend)

## Jalankan

```bash
# Terminal 1 — Backend
cd backend && cargo run

# Terminal 2 — Frontend
cd frontend && npm install && npm run dev

Buka http://localhost:5173
